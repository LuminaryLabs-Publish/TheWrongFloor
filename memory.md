# Wrong Floor memory

## Active intent — 2026-09-05
- Final user steering: "forget about mac for now and call it complete with windows and web and linux." This pass is COMPLETE for Windows, Web, and Linux. macOS is deferred and removed from active candidate CI; optional packaging support is retained.
- Improve unpleasant audio while preserving the direction of the current recordings.
- Increase fear and visual fidelity without losing the grainy PS2 aesthetic.
- Deliver a focused, replayable five-minute vertical slice aimed at a $0.99 product.
- Original request included Windows, macOS, Linux, and Web; the final accepted scope is Windows, Linux, and Web. Do not equate a package with tested commercial readiness.

## Baseline findings
- Clean Git worktree at start. Local Three.js r165 game, deterministic 30-floor / 300-second rules, worker-generated geometry, Electron wrapper.
- Initial hypothesis from code: music gain 1.65 and tiny machinery gain suggested a mix imbalance. Later decoded-sample measurement superseded that hypothesis: the active music file is almost silent (RMS ~0.00038 before conditioning), an alternate cannot decode, and the older loop is only two seconds. Do not infer source loudness from gain alone.
- Approach played bones and a full jumpscare together, with breathing/heartbeat/scream overlapping. Impact transients were buried behind ~2.6 and ~3.6 seconds of quiet lead-in. Samples had no level conditioning, tonal filtering, spatialization, or loop seam treatment.
- A continuous CSS film overlay exists; lighting uses a strong global fill. Preserve retro character while improving depth and material separation.
- Desktop packaging explicitly accepts only Linux and Windows. Browser test scripts use POSIX environment assignment and Linux-only browser discovery.
- Audio provenance lists source filenames but does not record licenses for supplied samples. Record this concrete distribution gap; do not invent clearance.
- Existing browser tools and local build tools cover implementation and inspection. No additional plugin installation is needed.

## Work and evidence
- Applying do-it and human-game-view-expert skills; control-in-app-browser for visible inspection.
- Implemented original deterministic 16-second music-box phrase, conservative sample conditioning and onset alignment, smooth loop joins, hallway lowpass/attenuation, music ducking, controlled breathing/heartbeat, mechanical foley, and death-only sting. Reset/seal/retry cancel stale cues.
- Implemented native-resolution UI over 360p/480p/720p rendering, color grading, tonal quantization, adjustable grain/brightness, practical lighting/depth, floor wear, subtle shafts/dust, and cabin details. The initial cone-shaped shafts were softened after visual review.
- Added title survival brief, door closure percentage/progress, saved picture controls, original PNG/ICO/ICNS launcher icons, portable archive/checksum generator, and native build CI for all requested platforms.
- Node regression suite: 20 tests pass, including the original deterministic rules and new audio/save behavior.
- Browser candidate review passed all 12 variants, both failures, 300-second scripted escape, actual keyboard close/pause, WebGL/local resources, and no browser errors. Measured software-rendered default profile ~30 FPS on this Windows host; not a target-hardware performance promise.
- Final offline audio render: 8/8 assets decoded, no clipped samples, peak ~0.266 (about -11.5 dBFS). Corrected music RMS ~0.104 after conditioning. Preview and report under `_review/final/`.
- Windows x64 package was executed successfully using its secure custom protocol. Isolated native smoke passed WebGL, all audio loads, real keyboard closure/pause, renderer isolation, and Quit text. Evidence: `_review/native/native-validation.json` and `native-gameplay.png`.
- Linux x64 cross-package created; Linux runtime untested. macOS packaging attempted for arm64 and x64 but Electron Packager cannot create required symlinks on this Windows host. Native CI and Mac package commands exist; no macOS binaries or native verification are claimed.
- Desktop dependencies downloaded from the pinned npm/Electron sources after standard sandbox network access failed. The system npm shim is broken; invoking `C:/Program Files/nodejs/node_modules/npm/bin/npm-cli.js` directly works. No user-wide configuration was changed.
- Seven supplied voice/impact recordings still lack commercial license evidence. Mac signing/notarization and native Mac/Linux playtests remain external release work. No publishing, signing, store submission, or public release performed.
- Final real-time browser acceptance completed successfully: 305.8 wall seconds, exactly 300 active seconds, all 30 correct decisions, zero mistakes, all 12 variants and both failure paths covered separately. Full-session software rendering averaged 27.05 FPS with capture overhead. Report: `_review/final/validation.json`; trace and screenshots accompany it.
- Final Windows binary was rebuilt and its native check repeated. The report records the exact `app.asar` SHA-256; the Windows release manifest confirms that tested hash matches the archived executable payload.
- Final distributables: `releases/wrong-floor-0.2.0-win32-x64.zip` (154.9 MiB), `releases/wrong-floor-0.2.0-linux-x64.zip` (119.8 MiB), and `releases/wrong-floor-0.2.0-web.zip` (1.0 MiB). Each archive passed independent Python ZIP CRC checks, every manifest file hash, and its archive checksum. Web entry is at the ZIP root; Linux executable permissions are preserved.
- `npm test` passed all 20 tests, source/import validation, current 118-file migration manifest, and 76-file desktop stage verification. Web/native preview images were visually inspected. Audio preview was numerically reviewed, not human-listening certified.

- Follow-up: user asked how to start the Web build. Launch from the repository with node scripts/serve.mjs, then open http://127.0.0.1:4173/game/. Rebuild with node scripts/build-site.mjs after source changes.

- Follow-up installation guidance: added docs/INSTALL.md for portable Windows/Linux players, local Web setup from the repository (Node 22.12+; no npm install needed), and standalone Web ZIP hosting. Linked it from README. Desktop archives include the runtime; Linux native verification remains pending. Existing tools cover this documentation task; no additional plugin is needed.

## 2026-09-05 — Itch media pack
User requested exactly ten gameplay screenshots and a short edited gameplay demonstration video, uploaded to Google Drive. Use real rendered gameplay with original synchronized audio and preserve the PS2 grain. Existing QA footage is low-frame-rate and silent, so new capture is needed. Google Drive connector is available; no additional plugin needed. Windows, Linux and Web remain the target platforms; macOS is deferred.

Media pass completed: ten 1920x1080 PNG screenshots (title, office, hotel, basement, all six entity families) and a 36-second 1280x720/30 FPS H.264 + AAC stereo gameplay demonstration. Sources recorded around 60 FPS with synchronized post-master game audio; edit contains six real-time gameplay takes and a three-second end slate. Final audio measures -20.13 LUFS integrated, -1.50 dBTP. In-app browser played the entire video to 36 seconds with readyState 4 and no error. Local delivery: releases/wrong-floor-media-0.2.0/. Drive folder: https://drive.google.com/drive/folders/1sudhVuwzQtpMGU3KD9XtA7EFOwThpb5b . Drive readback verified all ten PNGs and one MP4, matching local byte sizes and remaining unshared. Video: https://drive.google.com/file/d/14om6Dwsh4X08z4C4ZAKwpLQh-W1v7ToK/view . Capture-only harness scripts/capture-media.mjs serves review clock and audio taps without changing shipped game sources; screenshots use shipped High quality and video Medium, original grain at 35%. Raw takes, contact sheets, encoder, final validation and Drive readback are in _review/media-capture and _review/media-tools. An optional internal build-manifest upload was rejected by automatic approval review as outside the authorized media payload; it was excluded, not retried. One screenshot upload had a transient connector error; confirmed absence before retry, then verified exactly 11 delivered files. No files were published to itch and no sharing permissions were changed.

User requested media access for admin@luminarylabs.com. Sharing attempt failed: Google reports no Google account associated with this address and requires a notification invitation; available Drive share connector forces sendNotificationEmail=false. Browser is signed in as admin@luminarylabs.dev and cannot access the owning connector's folder. No access was granted and no permissions were broadened. Need recipient clarification (.com versus observed .dev), or owner-account browser access to send a visitor invitation.

User confirmed corrected recipient admin@luminarylabs.dev. Granted reader access to all eleven media files via Drive connector; readback of each file's permissions verified this exact email with reader role. Folder itself was not shared because connector does not support folder sharing and browser account lacks folder-owner access. All ten screenshots and gameplay MP4 are individually accessible to admin@luminarylabs.dev; no public or domain-wide permissions added.

User asked which files to upload on itch for each build. Verified actual ZIPs: Web 110 entries with index.html at archive root; Windows 76 entries including wrong-floor.exe and bundled Electron runtime; Linux 75 entries including wrong-floor and runtime. Upload existing complete ZIP per platform unchanged; label Windows/Linux downloads and mark only Web ZIP playable in browser under HTML Game. Recommend fullscreen click-to-launch, retain runtime/support files, exclude separate manifests/checksums from required runtime uploads. Itch official HTML5 docs confirm relative paths and root index requirements, and embedded HTML5 payments are donations rather than a mandatory purchase gate; paid access requires downloadable project type. Linux native validation remains outstanding.
