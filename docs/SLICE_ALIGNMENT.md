# Development alignment

`main` is the only active Wrong Floor development branch.

Frozen milestone branches:

- `0.0.1` — initial independent vertical slice.
- `0.0.2` — Floor 30 / elevator presentation milestone.
- `0.0.3` — expanded authored vertical slice.

The next milestone is **0.0.4 — Polished Descent**, but no `0.0.4` branch exists until the milestone is proven. Work toward it happens directly on `main`.

The canonical Master GDD remains the design authority. Historical branch behavior is evidence, not automatic design authority.
